default: &default
   adapter: postgresql
   encoding: unicode

development:
   adapter: postgresql
   encoding: unicode
   database: restro
   username: postgres
   password: admin

test:
   adapter: postgresql
   encoding: unicode
   database: library_test
   username: postgres
   password: admin

production:
   adapter: postgresql
   encoding: unicode
   database: library_production
   username: postgres
   password: admin
